<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.knockknock.mapper.MeetingAndEventMapper">

<!--     <select id="meetingCount" resultType="int"> -->
<!--         SELECT -->
<!--             COUNT(*) -->
<!--         FROM meeting -->
<!--     </select> -->
    <select id="meetingList" resultType="MeetingVDTO">
        SELECT
            MEETING.WRITING_NUMBER WRITINGNUMBER, MEETING.TITLE, MEETING.WRITE_TIME WRITETIME, MEETING.HIT, MEMBER.NICKNAME
        FROM MEETING, MEMBER
        WHERE MEETING.MEMBER_NUMBER=MEMBER.MEMBER_NUMBER
        ORDER BY WRITING_NUMBER DESC
    </select>

</mapper>