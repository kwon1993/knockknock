<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.knockknock.mapper.MemberMapper">
    <insert id="register">
        insert into member(member_number,grade,email,password,phone_number,nickname,name,birth,gender,join_Time,address,address_Detail)
		values
		(0, '일반회원', #{email}, #{password},#{phoneNumber}, #{nickname}, #{name}, now(), #{gender}, now(), #{address}, #{addressDetail})
    </insert>

	<select id="checkEmail" resultType="MemberDTO">
		select
		M.EMAIL email
		from member M
		where email = #{email}
	</select>
	
    <select id="findById" resultType="MemberDTO">
    	select email,password,grade,name from member 
    	where email = #{id}
    </select>
    
    <select id="findByEmail" resultType="MemberDTO">
    	select email, password, grade, name from member 
    	where email = #{email}
    </select>
    
    <select id="findByName" resultType="MemberDTO">
    	select 
    	M.EMAIL email,
    	M.NAME name
    	from member M
    	where 
    	M.NAME = #{name}
    	AND M.PHONE_NUMBER = #{phoneNumber}
    </select>
    
    <update id="changePassword">
    	update member 
		set password=#{password}
		where email=#{email}
    </update>
    
    <select id="getProfile" resultType="ProfileVDTO">
    	SELECT
    	M.MEMBER_NUMBER memberNumber,
    	M.NAME,
    	P.ANIMAL,
    	P.AMOUNT
    	FROM MEMBER M, PET P
    	where M.EMAIL = #{username}
    	AND M.MEMBER_NUMBER = P.MEMBER_NUMBER 
    </select>

    <select id="getMML" resultType="MeetingDTO">
    	SELECT
    	FROM 
    	WHERE
    </select>
</mapper>